# Code Analysis
networkx>=3.0

# Multi-language Parsing
tree-sitter>=0.21.0
tree_sitter_language_pack>=0.6.0

# Path matching (optional .gitignore support)
pathspec>=0.12

# YAML parsing (for native_functions.yaml)
pyyaml>=6.0

# MCP Server (for Claude Code integration)
mcp>=1.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
